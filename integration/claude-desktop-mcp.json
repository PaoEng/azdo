{
  "_comment": "Drop into Claude Desktop config (~/Library/Application Support/Claude/claude_desktop_config.json on macOS, %APPDATA%\\Claude\\claude_desktop_config.json on Windows). Reuses the same azdo mcp serve binary.",
  "mcpServers": {
    "azdo": {
      "command": "/usr/local/bin/azdo",
      "args": ["mcp", "serve"]
    }
  }
}
